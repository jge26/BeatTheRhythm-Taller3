import java.io.FileNotFoundException;

public class ListaInstrumentos {
/** me perdi y no supe como hacerlo
 *
    private listaInstrumentos[] instumento;
    private int max;
    private int cantActual;

    public listaInstrumentos(int max) {
        this.max = max;
        this.cantActual = 0;
        this.instumento = new listaInstrumentos[max];
    }
    public void recorrerLista(){
        for(int i = 0; i < this.cantActual; i++){
            System.out.println(this.instumento[i].getidInstrumento());
        }
    }

    public void agregarInstrumento(listaInstrumento p){
        if(this.cantActual == this.max){
            return;
        }
        this.instrumento[this.cantActual] = p;
        this.cantActual++;
    }
 **/
}
